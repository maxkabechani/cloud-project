services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: hpc
      POSTGRES_PASSWORD: local-development-only
      POSTGRES_DB: hpc_cloud
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hpc -d hpc_cloud"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  postgres-data:
